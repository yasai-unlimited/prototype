<p id="notice"><%= notice %></p>

<h1>Listing Families</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Friend open</th>
      <th>General open</th>
      <th>Icon</th>
      <th>Coverimage</th>
      <th>Description</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @families.each do |family| %>
      <tr>
        <td><%= family.name %></td>
        <td><%= family.friend_open %></td>
        <td><%= family.general_open %></td>
        <td><%= family.icon %></td>
        <td><%= family.coverimage %></td>
        <td><%= family.description %></td>
        <td><%= family.title %></td>
        <td><%= link_to 'Show', family %></td>
        <td><%= link_to 'Edit', edit_family_path(family) %></td>
        <td><%= link_to 'Destroy', family, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Family', new_family_path %>
